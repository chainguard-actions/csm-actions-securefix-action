name: Test notify action mode
on:
  workflow_dispatch:
jobs:
  test-notify-mode:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          action: notify
          app_id: "12345"
          app_private_key: |
            -----BEGIN RSA PRIVATE KEY-----
            MIIEowIBAAKCAQEA0Z3VS5JJcds3xHn/ygWep4PAtEsHABQbXQMuMFMNbkBMCMJA
            -----END RSA PRIVATE KEY-----
          pull_request_comment: "Security fix has been applied successfully."

      - name: Assert notify mode ran (fails gracefully without real credentials/repo)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: notify mode succeeded"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: notify mode failed gracefully (expected without real credentials)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
